# SyncThink HRMS — Web (Next.js)

Frontend app. Not yet scaffolded with actual Next.js source — this is a
workspace placeholder so the monorepo installs cleanly.

## Next step
Run `npx create-next-app@latest .` inside this folder (choosing TypeScript,
Tailwind, App Router) once you're ready to start Phase 4 of the roadmap,
or ask Claude to scaffold the real `src/app` structure.
